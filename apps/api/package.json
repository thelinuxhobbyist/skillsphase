{
  "name": "@horizon/api",
  "version": "0.0.1",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "build": "wrangler deploy --dry-run --outdir=dist",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "lint": "echo \"No lint configured for @horizon/api\"",
    "cf-typegen": "wrangler types"
  },
  "dependencies": {
    "@clerk/backend": "^2.4.0",
    "@horizon/database": "workspace:*",
    "@horizon/shared": "workspace:*",
    "hono": "^4.8.3",
    "zod": "^3.25.67"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.24.0"
  }
}
